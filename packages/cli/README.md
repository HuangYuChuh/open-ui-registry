# CLI package

Reserved for installation and maintenance workflows that cannot be expressed through the standard shadcn CLI.

Version 1 will prefer the shadcn Registry protocol. This package should remain empty until a concrete product need justifies a custom command.
